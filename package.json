{
  "name": "funk-lib",
  "version": "0.2.0",
  "engines": {
    "node": "8.9.1"
  },
  "description": "Functional JavaScript Utilities",
  "author": "Colby Dauphinais <colby@dauphina.is> (https://colby.dauphina.is)",
  "contributors": [
    "Colby Dauphinais <colby@dauphina.is> (https://colby.dauphina.is)"
  ],
  "dependencies": {
    "ramda": "~0.25.0"
  },
  "devDependencies": {
    "chai": "~4.1.2",
    "chai-as-promised": "~7.1.1",
    "eslint": "~4.19.1",
    "eslint-config-defaults": "github:colbydauph/eslint-config-defaults#1.1.0",
    "mocha": "~5.2.0",
    "nyc": "~11.8.0",
    "sinon": "~5.0.10"
  },
  "scripts": {
    "init": "rm -rf node_modules && npm install",
    "test": "mocha --reporter=list --require=./tests/bootstrap-test.js $(find . ! -path '*node_modules*' -name '*.test.js') --",
    "test:watch": "mocha --watch --reporter=min --require=./tests/bootstrap-test.js $(find . ! -path '*node_modules*' -name '*.test.js') --",
    "lint": "eslint --max-warnings=10 $(find . ! -path '*node_modules*' ! -path '*dist*' ! -path '*coverage*' -type f | egrep '[.]js?$')",
    "cover": "nyc --check-coverage npm test",
    "verify": "npm run lint && npm run cover"
  },
  "nyc": {
    "statements": 90,
    "branches": 75,
    "functions": 80,
    "lines": 95,
    "reporter": [
      "html",
      "text-summary"
    ],
    "watermarks": {
      "statements": [
        90,
        95
      ],
      "branches": [
        90,
        95
      ],
      "functions": [
        90,
        95
      ],
      "lines": [
        90,
        95
      ]
    }
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/colbydauph/funk-lib.git"
  },
  "bugs": {
    "url": "https://github.com/colbydauph/funk-lib/issues"
  },
  "private": true,
  "license": "MIT"
}
